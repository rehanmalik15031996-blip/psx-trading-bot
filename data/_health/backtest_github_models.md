# Layer 2 -- GPT-4o-mini brain backtest (SKIPPED)

_Run at 2026-05-03T11:34:24_

**Skipped:** GITHUB_TOKEN env var missing (need 'models:read' scope).

To enable this layer:

1. Create a fine-grained PAT at https://github.com/settings/tokens with the `models:read` scope.
2. Set `GITHUB_TOKEN=<your token>` in your shell.
3. Re-run `python scripts/backtest_with_github_models.py`.
