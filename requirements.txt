# Streamlit Cloud runtime requirements (slim).
#
# This file is consumed by Streamlit Cloud at deploy time. The dashboard
# only READS pre-computed parquet / JSON artefacts written by the heavy
# GitHub Actions pipelines (overnight, eod, predictions, news_scoring,
# fundamentals, financial_results, material_info, macro_kpis). It never
# trains a model on demand, so we deliberately leave out lightgbm /
# catboost / torch / transformers / vectorbt / shap — that keeps the
# deployed image well below the free tier's 1 GB memory ceiling and
# makes redeploys complete in ~3 minutes instead of ~12.
#
# For local development / training, use ``requirements-full.txt``
# instead:
#     pip install -r requirements-full.txt

# Core data + UI
streamlit>=1.38.0
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=16.0.0
plotly>=5.22.0

# Connectors at runtime (sector lookups, fundamentals snapshots,
# news fetch, fair-value calc)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
feedparser>=6.0.10
yfinance>=0.2.40
python-dateutil>=2.8.2
rich>=13.7.0

# Technicals (Bollinger / MACD / OBV / Stoch RSI in the briefing
# preview that the UI surfaces inside chatbot answers)
ta>=0.11.0
scikit-learn>=1.4.0

# LLM clients — chatbot tool dispatcher selects the live provider
anthropic>=0.34.0
openai>=1.40.0
google-genai>=1.0.0

# Daily PDF brief (analyst-facing report download)
reportlab>=4.0.0
pypdf>=4.0.0
